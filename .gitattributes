# git autocrlf=true converts LF to CRLF on Windows, causing issues with oxfmt
* text=auto eol=lf

# Batch wrappers are only ever run by cmd.exe, which is happiest with CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf
